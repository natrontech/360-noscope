noscope360:
  elastic:
    host: 'localhost'
    port: 9200
    username: 'elastic'
    password: 'secret'

spring:
  mvc:
    problemdetails:
      enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /api-docs-ui.html

admin:
  security:
    username: admin
    password: s3cr3t

server:
  port: 8080
